name: 'Code Check Workflow'

# technically this always runs in Pull rquests, but nowhere else.
on:
  workflow_dispatch:
jobs:
  runs-on: ubuntu-latest
  steps:
    - name: checkout
      uses: actions/checkout@v4
    - name: Code checks pipeline
      uses: ./github/actions/dummy.yml
      with:
        lib-path: .
